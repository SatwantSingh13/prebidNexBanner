{
  "name": "nexbid-prebid-adapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "NexBid client-side bidder adapter package for Prebid.js submission.",
  "scripts": {
    "check": "node --check modules/nexbidBidAdapter.js && node --check public/prebid-renderer.js && node --check functions/api/v1/prebid/bid.js",
    "test:gateway": "node --test test/server/prebidBidEndpoint.test.js"
  }
}
